---
interface CategoryCard {
  title: string;
  description: string;
  href: string;
  color: string;
  iconBgColor: string;
  emoji: string;
  radialGradient: string;
}

const categories: CategoryCard[] = [
  {
    title: 'Blog',
    description: '思考观点 · 技术笔记',
    href: '/blog',
    color: '#7C3AED',
    iconBgColor: '#6D28D9',
    emoji: '📝',
    radialGradient: 'radial-gradient(circle at 30% 40%, rgba(168,85,247,0.3), transparent 60%)'
  },
  {
    title: 'Projects',
    description: '案例展示 · 开源作品',
    href: '/projects',
    color: '#0891B2',
    iconBgColor: '#077A96',
    emoji: '💼',
    radialGradient: 'radial-gradient(circle at 70% 30%, rgba(6,182,212,0.3), transparent 60%)'
  },
  {
    title: 'Videos',
    description: '教程资源 · 分享推荐',
    href: '/videos',
    color: '#EA580C',
    iconBgColor: '#D14A0A',
    emoji: '🎥',
    radialGradient: 'radial-gradient(circle at 50% 60%, rgba(249,115,22,0.3), transparent 60%)'
  }
];

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<section class:list={["pb-16 md:pb-[64px]", className]}>
  <div class="container-medium">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {categories.map((category, index) => (
        <a
          href={category.href}
          class:list={[
            "group flex flex-col rounded-2xl overflow-hidden h-[300px]",
            "transition-all duration-300 ease-out",
            "hover:-translate-y-2",
            "opacity-0-init animate-slide-up"
          ]}
          style={{
            backgroundColor: category.color,
            animationDelay: `${500 + index * 100}ms`,
            boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1), inset 0 -1px 0 rgba(255,255,255,0.1)'
          }}
          onmouseenter="this.style.boxShadow='0 20px 40px -10px rgba(0, 0, 0, 0.3), inset 0 -1px 0 rgba(255,255,255,0.1)';"
          onmouseleave="this.style.boxShadow='0 4px 6px -1px rgba(0, 0, 0, 0.1), inset 0 -1px 0 rgba(255,255,255,0.1)';"
        >
          {/* Icon area - 60% height with darker background and radial gradient */}
          <div
            class="h-[60%] flex items-center justify-center transition-transform duration-300 group-hover:scale-105 relative overflow-hidden"
            style={{
              backgroundColor: category.iconBgColor,
              backgroundImage: category.radialGradient
            }}
          >
            <span
              class="text-6xl md:text-[64px]"
              style="filter: grayscale(100%) contrast(1.2);"
            >
              {category.emoji}
            </span>
          </div>

          {/* Text content - 40% height */}
          <div class="flex-1 flex flex-col justify-center p-6">
            <h3
              class="font-bold text-white mb-2"
              style="font-family: var(--font-display) !important; font-size: 28px;"
            >
              {category.title}
            </h3>
            <p class="text-white/80 text-sm md:text-base">
              {category.description}
            </p>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

---
interface CategoryCard {
  title: string;
  description: string;
  href: string;
  color: string;
  iconBgColor: string;
  emoji: string;
}

const categories: CategoryCard[] = [
  {
    title: 'Blog',
    description: '思考观点 · 技术笔记',
    href: '/blog',
    color: '#7C3AED',
    iconBgColor: '#6D28D9',
    emoji: '📝'
  },
  {
    title: 'Projects',
    description: '案例展示 · 开源作品',
    href: '/projects',
    color: '#0891B2',
    iconBgColor: '#077A96',
    emoji: '💼'
  },
  {
    title: 'Videos',
    description: '教程资源 · 分享推荐',
    href: '/videos',
    color: '#EA580C',
    iconBgColor: '#D14A0A',
    emoji: '🎥'
  }
];

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<section class:list={["pb-20 md:pb-[80px]", className]}>
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {categories.map((category, index) => (
        <a
          href={category.href}
          class:list={[
            "group flex flex-col rounded-2xl overflow-hidden h-[300px]",
            "transition-all duration-300 ease-out",
            "hover:-translate-y-2",
            "opacity-0-init animate-slide-up"
          ]}
          style={{
            backgroundColor: category.color,
            animationDelay: `${500 + index * 100}ms`,
            boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1)'
          }}
          onmouseenter="this.style.boxShadow='0 20px 40px -10px rgba(0, 0, 0, 0.3)';"
          onmouseleave="this.style.boxShadow='0 4px 6px -1px rgba(0, 0, 0, 0.1)';"
        >
          {/* Icon area - 60% height with darker background */}
          <div
            class="h-[60%] flex items-center justify-center transition-transform duration-300 group-hover:scale-105"
            style={{ backgroundColor: category.iconBgColor }}
          >
            <span
              class="text-6xl md:text-[64px]"
              style="filter: grayscale(100%) contrast(1.2);"
            >
              {category.emoji}
            </span>
          </div>

          {/* Text content - 40% height */}
          <div class="flex-1 flex flex-col justify-center p-6">
            <h3
              class="font-serif font-bold text-white mb-2"
              style={{ fontSize: '28px' }}
            >
              {category.title}
            </h3>
            <p class="text-white/80 text-sm md:text-base">
              {category.description}
            </p>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

---
interface CategoryCard {
  title: string;
  description: string;
  href: string;
  color: string;
  emoji: string;
}

const categories: CategoryCard[] = [
  {
    title: 'Blog',
    description: '思考观点 · 技术笔记',
    href: '/blog',
    color: '#7C3AED',
    emoji: '📝'
  },
  {
    title: 'Projects',
    description: '案例展示 · 开源作品',
    href: '/projects',
    color: '#0891B2',
    emoji: '💼'
  },
  {
    title: 'Videos',
    description: '教程资源 · 分享推荐',
    href: '/videos',
    color: '#EA580C',
    emoji: '🎥'
  }
];

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<section class:list={["py-8 md:py-12", className]}>
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {categories.map((category, index) => (
        <a
          href={category.href}
          class:list={[
            "group flex flex-col rounded-2xl p-8 h-[280px]",
            "transition-all duration-300 ease-out",
            "hover:-translate-y-2",
            "opacity-0-init animate-slide-up"
          ]}
          style={{
            backgroundColor: category.color,
            animationDelay: `${500 + index * 100}ms`,
            boxShadow: '0 4px 6px -1px rgba(0, 0, 0, 0.1)'
          }}
          onmouseenter="this.style.boxShadow='0 20px 40px -10px rgba(0, 0, 0, 0.3)';"
          onmouseleave="this.style.boxShadow='0 4px 6px -1px rgba(0, 0, 0, 0.1)';"
        >
          {/* Emoji placeholder - top 60% */}
          <div class="h-[60%] flex items-center justify-center">
            <span
              class="text-6xl md:text-7xl transition-transform duration-300 group-hover:scale-105"
              style="filter: grayscale(100%) contrast(1.2);"
            >
              {category.emoji}
            </span>
          </div>

          {/* Text content - bottom 40% */}
          <div class="flex-1 flex flex-col justify-end">
            <h3 class="font-serif text-2xl md:text-3xl font-bold text-white mb-2">
              {category.title}
            </h3>
            <p class="text-white/80 text-sm md:text-base">
              {category.description}
            </p>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

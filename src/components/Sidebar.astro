---
import { getCollection } from 'astro:content';
import { SITE_TITLE, SITE_DESCRIPTION, GITHUB_URL, TWITTER_URL } from '../utils/constants';

// Get data for sidebar
const posts = await getCollection('blog', ({ data }) => !data.draft);
const recentPosts = posts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 5);

const allCategories = [...new Set(posts.flatMap(p => p.data.categories))];
const categoryCounts = allCategories.map(cat => ({
  name: cat,
  count: posts.filter(p => p.data.categories.includes(cat)).length
}));

const allTags = [...new Set(posts.flatMap(p => p.data.tags))];
const topTags = allTags.slice(0, 10);

function formatDate(date: Date) {
  return new Intl.DateTimeFormat('zh-CN', {
    month: 'short',
    day: 'numeric',
  }).format(date);
}
---

<aside class="w-full lg:w-[280px] space-y-6">
  <!-- Profile Card -->
  <div class="bg-[var(--bg-secondary)] rounded-xl p-5 border border-[var(--border-color)]">
    <div class="flex items-center gap-3 mb-3">
      <div class="w-12 h-12 rounded-full bg-[var(--bg-tertiary)] flex items-center justify-center text-xl">
        🚀
      </div>
      <div>
        <h3 class="font-semibold">{SITE_TITLE}</h3>
        <p class="text-xs text-[var(--text-muted)]">开发者 / 博主</p>
      </div>
    </div>
    <p class="text-sm text-[var(--text-secondary)] mb-4">
      {SITE_DESCRIPTION}
    </p>
    <div class="flex gap-3">
      <a
        href={GITHUB_URL}
        target="_blank"
        rel="noopener noreferrer"
        class="p-2 text-[var(--text-muted)] hover:text-[var(--text-primary)] hover:bg-[var(--bg-tertiary)] rounded-lg transition-colors"
        aria-label="GitHub"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"></path>
        </svg>
      </a>
      <a
        href={TWITTER_URL}
        target="_blank"
        rel="noopener noreferrer"
        class="p-2 text-[var(--text-muted)] hover:text-[var(--text-primary)] hover:bg-[var(--bg-tertiary)] rounded-lg transition-colors"
        aria-label="Twitter"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M22 4s-.7 2.1-2 3.4c1.6 10-9.4 17.3-18 11.6 2.2.1 4.4-.6 6-2C3 15.5.5 9.6 3 5c2.2 2.6 5.6 4.1 9 4-.9-4.2 4-6.6 7-3.8 1.1 0 3-1.2 3-1.2z"></path>
        </svg>
      </a>
      <a
        href="/rss.xml"
        class="p-2 text-[var(--text-muted)] hover:text-[var(--text-primary)] hover:bg-[var(--bg-tertiary)] rounded-lg transition-colors"
        aria-label="RSS"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M4 11a9 9 0 0 1 9 9"></path>
          <path d="M4 4a16 16 0 0 1 16 16"></path>
          <circle cx="5" cy="19" r="1"></circle>
        </svg>
      </a>
    </div>
  </div>

  <!-- Categories -->
  {categoryCounts.length > 0 && (
    <div class="bg-[var(--bg-secondary)] rounded-xl p-5 border border-[var(--border-color)]">
      <h3 class="font-semibold mb-4 text-sm uppercase tracking-wider text-[var(--text-muted)]">分类</h3>
      <ul class="space-y-2">
        {categoryCounts.map(({ name, count }) => (
          <li>
            <a
              href={`/blog/categories/${name.toLowerCase()}`}
              class="flex items-center justify-between text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors group"
            >
              <span>{name}</span>
              <span class="text-xs text-[var(--text-muted)] bg-[var(--bg-tertiary)] px-2 py-0.5 rounded-full group-hover:bg-[var(--bg-primary)]">
                {count}
              </span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  )}

  <!-- Tags -->
  {topTags.length > 0 && (
    <div class="bg-[var(--bg-secondary)] rounded-xl p-5 border border-[var(--border-color)]">
      <h3 class="font-semibold mb-4 text-sm uppercase tracking-wider text-[var(--text-muted)]">标签</h3>
      <div class="flex flex-wrap gap-2">
        {topTags.map(tag => (
          <a
            href={`/blog/tags/${tag.toLowerCase()}`}
            class="text-xs px-2 py-1 bg-[var(--bg-tertiary)] text-[var(--text-secondary)] rounded hover:bg-[var(--bg-primary)] hover:text-[var(--text-primary)] transition-colors"
          >
            #{tag}
          </a>
        ))}
      </div>
    </div>
  )}

  <!-- Recent Posts -->
  {recentPosts.length > 0 && (
    <div class="bg-[var(--bg-secondary)] rounded-xl p-5 border border-[var(--border-color)]">
      <h3 class="font-semibold mb-4 text-sm uppercase tracking-wider text-[var(--text-muted)]">最新文章</h3>
      <ul class="space-y-3">
        {recentPosts.map(post => (
          <li>
            <a
              href={`/blog/${post.slug}`}
              class="block group"
            >
              <h4 class="text-sm font-medium text-[var(--text-secondary)] group-hover:text-[var(--text-primary)] transition-colors line-clamp-2">
                {post.data.title}
              </h4>
              <span class="text-xs text-[var(--text-muted)] mt-1">
                {formatDate(post.data.date)}
              </span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  )}
</aside>

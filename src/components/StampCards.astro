---
---

<section class="stamps-section">
  <div class="stamps-container">
    <a href="/blog" class="stamp stamp-blog" style="--default-rotation: -6deg;">
      <img src="/assets/images/stamp-blog.png" alt="Blog">
    </a>
    <a href="/projects" class="stamp stamp-projects" style="--default-rotation: 4deg;">
      <img src="/assets/images/stamp-projects.png" alt="Projects">
    </a>
    <a href="/videos" class="stamp stamp-videos" style="--default-rotation: -3deg;">
      <img src="/assets/images/stamp-videos.png" alt="Videos">
    </a>
  </div>
</section>

<script is:inline>
  // 入场动画 - 使用 Intersection Observer 当邮票滚入视口时触发
  const stampsObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const stamps = entry.target.querySelectorAll('.stamp');
        stamps.forEach(stamp => {
          stamp.classList.add('animate-in');
        });
        stampsObserver.disconnect();
      }
    });
  }, { threshold: 0.2 });

  const stampsSection = document.querySelector('.stamps-section');
  if (stampsSection) {
    stampsObserver.observe(stampsSection);
  }
</script>

<style>
  .stamps-section {
    padding: 60px 0 80px;
  }

  .stamps-container {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0;
    max-width: 960px;
    margin: 0 auto;
    padding: 0 24px;
    perspective: 1000px;
  }

  /* 每张邮票 */
  .stamp {
    display: block;
    width: 320px;
    flex-shrink: 0;
    cursor: pointer;
    text-decoration: none;
    position: relative;
    /* 初始状态：隐藏，用于入场动画 */
    opacity: 0;
    transform: rotate(var(--default-rotation)) translateY(30px) scale(0.95);
    transition: all 0.6s cubic-bezier(0.23, 1, 0.32, 1);
  }

  .stamp.animate-in {
    opacity: 1;
    transform: rotate(var(--default-rotation)) translateY(0) scale(1);
    transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
  }

  /* 三张邮票交错出现的延迟 */
  .stamp-blog.animate-in {
    transition-delay: 0.1s;
  }

  .stamp-projects.animate-in {
    transition-delay: 0.3s;
  }

  .stamp-videos.animate-in {
    transition-delay: 0.5s;
  }

  .stamp img {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 4px;
    transition: filter 0.5s ease;
    mix-blend-mode: lighten;
  }

  /* 各张邮票的默认倾斜角度和叠压位置 */
  .stamp-blog {
    transform: rotate(-6deg) translateY(30px) scale(0.95);
    z-index: 1;
    margin-right: -30px;
  }

  .stamp-blog.animate-in {
    transform: rotate(-6deg) translateY(0) scale(1);
  }

  .stamp-projects {
    transform: rotate(4deg) translateY(30px) scale(0.95);
    z-index: 2;
    margin-top: 25px;
    margin-left: -30px;
    margin-right: -30px;
  }

  .stamp-projects.animate-in {
    transform: rotate(4deg) translateY(0) scale(1);
  }

  .stamp-videos {
    transform: rotate(-3deg) translateY(30px) scale(0.95);
    z-index: 1;
    margin-left: -30px;
    margin-top: -15px;
  }

  .stamp-videos.animate-in {
    transform: rotate(-3deg) translateY(0) scale(1);
  }

  /* ===== Hover 效果：摆正 + 放大 + 浮起 ===== */

  .stamp:hover {
    transform: rotate(0deg) scale(1.08) translateY(-8px) !important;
    z-index: 10;
    filter: drop-shadow(0 16px 32px rgba(0, 0, 0, 0.6));
  }

  /* 当某张邮票被 hover 时，其他邮票退后变暗 */
  .stamps-container:hover .stamp:not(:hover) {
    opacity: 0.55;
    filter: brightness(0.65);
    transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
  }

  /* 每张邮票底部添加微妙的品牌色投影 */
  .stamp-blog:hover {
    filter: drop-shadow(0 16px 32px rgba(124, 58, 237, 0.35));
  }

  .stamp-projects:hover {
    filter: drop-shadow(0 16px 32px rgba(8, 145, 178, 0.35));
  }

  .stamp-videos:hover {
    filter: drop-shadow(0 16px 32px rgba(234, 88, 12, 0.35));
  }

  /* ===== 移动端适配 ===== */

  @media (max-width: 768px) {
    .stamps-container {
      flex-direction: column;
      gap: 0;
      align-items: center;
    }

    .stamp {
      width: 260px;
      transform: rotate(0deg) translateY(30px) scale(0.95) !important;
    }

    .stamp.animate-in {
      transform: rotate(0deg) translateY(0) scale(1) !important;
    }

    .stamp:hover {
      transform: scale(1.05) translateY(-8px) !important;
    }

    .stamps-container:hover .stamp:not(:hover) {
      opacity: 0.7;
      transform: scale(1) translateY(0) !important;
    }

    /* 移动端不叠压 */
    .stamp-blog,
    .stamp-projects,
    .stamp-videos {
      margin: 0 !important;
      margin-bottom: 24px !important;
    }
  }

  @media (max-width: 480px) {
    .stamp {
      width: 220px;
    }
  }
</style>
